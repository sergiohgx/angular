module.exports = function(robot) {
  robot.hear(/hello/, function(res) {
    res.send("world!");
  });

  robot.respond(/hello/, function(res) {
    res.send("hello world " + res.message.user.name);
  });
